apply plugin: 'war'

allprojects {

    group = 'com.fortherosesocial'
    version = '1.0-SNAPSHOT'

    apply plugin: 'java'
    apply plugin: "groovy"

    repositories {
        jcenter()
    }

}

dependencies {
    compile project(":configurations")
}

